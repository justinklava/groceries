import path from 'path';
import {fileURLToPath} from 'url';

import { InsertReceipt } from '../config/db.js';

export { Insert };

const currentDirectory = path.dirname(fileURLToPath(import.meta.url));

function Insert(form) {
    
}
